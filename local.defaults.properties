# Copy this file to local.properties and fill in your real API keys.
# Values are read at build time by app/build.gradle.kts (secretKey() helper).
# Keys are deliberately empty here so builds never fail for developers without a local.properties.
OWM_API_KEY=
MAP_API_KEY=
API_KEY=
googleMapsKey=
MAPTILER_API_KEY=
IQAIR_API_KEY=
